using System;

namespace Implicit
{
    internal static class MathF
    {
        internal static float Log(float d)
        {
            return (float)Math.Log(d);
        }

        internal static float Sqrt(float d)
        {
            return (float)Math.Sqrt(d);
        }
    }
}
